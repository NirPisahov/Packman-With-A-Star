System Requests:
Java JDK-13.
Make sure the compilation level is 13 (in Eclipse project properties).
In Eclipse project properties > Java Compiler > Enable preview features (--enable-preview).

And you're ready to go!
